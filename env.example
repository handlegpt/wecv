# WeCV AI 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ===================
# 数据库配置 (MongoDB Atlas)
# ===================
# 方式1: 使用完整的 MongoDB Atlas 连接字符串 (推荐)
DB_HOST=mongodb+srv://wecv_user:your_password@cluster0.xxxxx.mongodb.net/wecv_ai?retryWrites=true&w=majority

# 方式2: 分别配置各个参数 (如果使用方式1，下面这些可以注释掉)
# DB_NAME=wecv_ai
# DB_USER=wecv_user
# DB_PASS=your_password
# DB_HOST=cluster0.xxxxx.mongodb.net
# DB_PORT=27017

# ===================
# Auth0 认证配置
# ===================
# 生成32位随机字符串作为密钥 (可以使用: openssl rand -hex 32)
AUTH0_SECRET=your_32_character_random_string_here

# 应用基础URL (本地开发使用 localhost:3066，生产环境使用你的域名)
AUTH0_BASE_URL=http://localhost:3066

# Auth0 域名 (从 Auth0 Dashboard 获取)
AUTH0_ISSUER_BASE_URL=https://your-domain.auth0.com

# 从 Auth0 Dashboard 应用设置中获取
AUTH0_CLIENT_ID=your_auth0_client_id
AUTH0_CLIENT_SECRET=your_auth0_client_secret

# ===================
# AWS S3 配置 (可选)
# ===================
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_S3_BUCKET=your_s3_bucket_name
AWS_S3_REGION=ap-south-1
AWS_S3_BUCKET_URL=https://your-bucket.s3.ap-south-1.amazonaws.com

# ===================
# Google Analytics (可选)
# ===================
NEXT_PUBLIC_GOOGLE_ANALYTICS=your_ga_id

# ===================
# 其他配置
# ===================
NODE_ENV=development
PORT=5000

# 后端API端点 (本地开发使用 localhost:5022，生产环境使用你的API域名)
NEXT_PUBLIC_BACKEND_API_ENDPOINT=http://localhost:5022/v1

# ===================
# 配置说明
# ===================
# 1. 复制此文件为 .env: cp env.example .env
# 2. 填入实际的配置值
# 3. 确保 .env 文件不会被提交到版本控制系统
# 4. 生产环境部署时，在服务器上设置相应的环境变量
#
# ===================
# MongoDB Atlas 设置步骤
# ===================
# 1. 访问 https://www.mongodb.com/atlas
# 2. 创建免费账户和集群
# 3. 创建数据库用户 (Database Access)
# 4. 设置网络访问 (Network Access) - 添加 0.0.0.0/0 允许所有IP
# 5. 获取连接字符串 (Connect -> Drivers -> Node.js)
# 6. 将连接字符串填入 DB_HOST 变量
#
# ===================
# Docker 部署说明
# ===================
# 使用 MongoDB Atlas 时，docker-compose.yml 已配置为：
# - 不安装本地 MongoDB 服务
# - 直接连接 MongoDB Atlas 云数据库
# - 节省服务器资源和存储空间
